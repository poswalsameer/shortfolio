import { createSlice } from "@reduxjs/toolkit";

const initialState = {
    username: ""
}

const userInfoSlice = createSlice({

    name: 'userInfo',
    initialState,
    reducers: {
        
    }

})